services:
  - type: web
    name: agentic-cicd-pipeline
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
